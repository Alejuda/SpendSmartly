<p style="color: green"><%= notice %></p>

<h1>Groups</h1>

<div id="groups">
  <% @groups.each do |group| %>
    <%= render group %>
    <p>
      <%= link_to "See group transactions", group_entities_path(group) %>
    </p>
  <% end %>
</div>

<%= link_to "New group", new_group_path %>
